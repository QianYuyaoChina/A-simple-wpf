<Window x:Class="MCPE_GUI.Directly_Connect"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:MCPE_GUI"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d" Height="195" Width="255"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Title="快速连接"
      >
    <Window.Resources>
        <!-- 圆角按钮样式 -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1890FF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                            CornerRadius="4"
                            BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid  Margin="15,15,15,5">
        <Label Content="选择服务器" Margin="0,0,0,5"/>
        <Button Content="花雨庭" Margin="16,32,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="80" Height="24" Click="FASTHYT" />
        <Button Content="布吉岛" Margin="112,32,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="80" Height="24" Click="FASTBJD" />
        <Button Content="EC" Margin="16,64,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="80" Height="24" Click="FASTEC" />
        <Button Content="租赁服" Margin="112,64,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="80" Height="24" Click="FASTZLF" />
        <Button Content="取消" Margin="65,98,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="80" Height="24" IsCancel="True" Click="cancel" />
    </Grid>
</Window>
